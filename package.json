{
  "name": "solarsim",
  "version": "1.0.0",
  "description": "Interactive educational physics sandbox — quarks to solar system",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix apps/universe-journey",
    "build": "npm run build --prefix apps/universe-journey",
    "install:all": "npm install --prefix apps/universe-journey"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

